using UnityEngine;
using System.Collections;

public class AIMovment : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        do
        {
            gameObject.GetComponent("Bla");
        } while (true);
    }
}
